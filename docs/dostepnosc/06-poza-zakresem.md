# Out of scope for now

**Kalendarz zewnętrzny (Google Calendar sync)** — "Połącz z Google", busy
times from a personal calendar suppress slots automatically. Needs OAuth,
token storage, a sync job/webhook — a real integration project on its own,
not a hackathon-timebox screen. Matches `plan.md`'s existing "Explicitly out
of scope" section in spirit (things bigger than the vertical slice). Revisit
only if steps 1–5 land with time to spare.
